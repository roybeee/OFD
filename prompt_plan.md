# 구현 계획 — 매출 분석 탭명 및 수동 5개년 백필

## 요구사항

- 본사와 점주 내비게이션의 `분석` 탭명을 `매출 분석`으로 변경한다.
- 과거 매출 백필은 자동 예약·반복·폴링으로 실행하지 않는다.
- POS가 연동된 각 매장에 `1년 백필`, `3년 백필`, `5년 백필` 수동 버튼을 제공한다.
- 사용자가 버튼을 누른 경우에만 해당 기간의 백필 작업을 시작한다.
- 장시간 HTTP 요청을 피하기 위해 백필은 월 단위 비동기 작업으로 수행한다.
- 작업 상태는 SQLite에 저장하되 서버 재시작 시 자동 재개하지 않는다.
- 중단된 동일 기간 작업은 사용자가 해당 버튼을 다시 누를 때 다음 구간부터 재개한다.
- 기존 오늘·어제 POS 자동 동기화는 과거 백필과 별개로 유지한다.

## 구현 순서

1. 탭명, 기간 상한, 권한, 중복 실행, 수동 재개, UI 버튼에 대한 실패 테스트를 추가한다.
2. 백필 작업 테이블과 월 단위 주문 수집·진행 상태 저장 로직을 구현한다.
3. 매장별 1년·3년·5년 버튼과 수동 상태 새로고침 UI를 구현한다.
4. 전체 통합 테스트, 코드 리뷰, 데스크톱·모바일 실화면 검증을 수행한다.
5. 실화면을 사용자에게 보여준 뒤 PR을 생성하고 병합은 대기한다.

## 위험 및 대응

- 토스플레이스 호출량 제한: 페이지 사이 간격을 유지하고 월 단위 범위로 나누어 호출한다.
- Render 재시작: 실행 중 작업을 `중단` 상태로 바꾸고 자동 재개하지 않는다.
- 중복 데이터: 기존 일 단위 전량 재계산 방식으로 멱등성을 유지한다.
- 장시간 작업: 버튼 요청에는 즉시 작업 ID를 반환하고 백그라운드에서 처리한다.
